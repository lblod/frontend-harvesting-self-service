<AuButtonGroup class="au-o-box" @inline={{true}}>
  <LinkTo @route="jobs.task.input-containers-files">
    <AuButton>
      List with input files
    </AuButton>
  </LinkTo>
  <LinkTo @route="jobs.task.results-containers-files">
    <AuButton >
     List with results files
    </AuButton>
  </LinkTo>
  <LinkTo @route="jobs.task.input-containers-graph">
    <AuButton >
     List with input graphs
    </AuButton>
  </LinkTo>
  <LinkTo @route="jobs.task.results-containers-graph" class="is-active">
    <AuButton @skin="secondary">
     List with results graphs
    </AuButton>
  </LinkTo>
</AuButtonGroup>

<AuHeading @skin="3" class="au-o-box">Graphs results list</AuHeading>

<AuDataTable
  @content={{this.model}}
  @noDataMessage="No graphs found"
  as |t|>
  <t.content as |c|>
    <c.header>
      <th class="au-u-visible-small-up">uri</th>
    </c.header>
    <c.body as |row|>
      <td class="au-u-visible-small-up">
        {{row.hasGraph}}
      </td>
    </c.body>
  </t.content>
</AuDataTable>

{{outlet}}
