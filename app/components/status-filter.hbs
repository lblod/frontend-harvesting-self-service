<PowerSelect
  @allowClear={{@allowClear}}
  @searchEnabled={{false}}
  @options={{this.statusOptions}}
  @selected={{@status}}
  @onChange={{@onChange}}
  class="filter-workaround"
  ...attributes
  as |status|>
  {{status-name status}}
</PowerSelect>
