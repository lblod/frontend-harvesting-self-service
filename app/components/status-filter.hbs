
    <PowerSelect
      @renderInPlace={{this.renderInPlace}}
      @searchEnabled={{false}}
      @options={{this.options}}
      @selected="Status Filter"
      @onChange={{fn (mut this.selected)}}
      as |singleselect|
    >
        <AuLink role="menuitem" @route="jobs.filter">
            {{singleselect}}
        </AuLink>
    </PowerSelect>
