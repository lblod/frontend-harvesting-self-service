<PowerSelect
  @allowClear={{@allowClear}}
  @searchEnabled={{false}}
  @options={{this.statusOptions}}
  @selected={{@status}}
  @onChange={{@onChange}}
  as |status|
>
  {{status-name status}}
</PowerSelect>
