<PowerSelect
  @allowClear={{@allowClear}}
  @renderInPlace={{this.renderInPlace}}
  @searchEnabled={{false}}
  @options={{this.statusOptions}}
  @selected={{@status}}
  @onChange={{@onChange}}
  as |status|
>
  {{status}}
</PowerSelect>
