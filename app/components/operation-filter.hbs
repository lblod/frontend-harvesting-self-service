<PowerSelect
  @allowClear={{@allowClear}}
  @searchEnabled={{false}}
  @options={{if @options @options this.jobOperations}}
  @selected={{@operation}}
  @onChange={{@onChange}}
  class="filter-large"
  ...attributes
  as |operation|
>
  {{operation-name operation}}
</PowerSelect>