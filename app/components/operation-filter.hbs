<PowerSelect
  @allowClear={{@allowClear}}
  @searchEnabled={{false}}
  @options={{this.jobOperations}}
  @selected={{@operation}}
  @onChange={{@onChange}}
  class="operation-filter"
  ...attributes
  as |operation|>
  {{operation}}
</PowerSelect>
