<PowerSelect
  @allowClear={{@allowClear}}
  @searchEnabled={{false}}
  @options={{this.jobOperations}}
  @selected={{@operation}}
  @onChange={{@onChange}}
  class="filter-workaround"
  ...attributes
  as |operation|>
  {{operation-name operation}}
</PowerSelect>
